# Tasks 006 — Responsive Buttons & shadcn/ui Button Alignment

- [x] `P6-01`: Refactor `apps/web/src/components/ui/button.tsx` to use `cva` (`buttonVariants`) with SLGA variants (`solid`, `surface`, `outline`, `ghost`, `destructive`), sizes (`default`, `hero`, `sm`, `lg`, `icon`), and polymorphic ref forwarding.
- [x] `P6-02`: Export `buttonVariants` in `apps/web/src/components/ui/index.ts`.
- [x] `P6-03`: Update `apps/web/src/components/sections/hero.tsx` button container and `ctaBase` to `flex-col sm:flex-row` and `w-full sm:w-auto`.
- [x] `P6-04`: Update `apps/web/src/app/not-found.tsx` buttons to `flex-col sm:flex-row` and `w-full sm:w-auto`.
- [x] `P6-05`: Update `apps/web/src/components/sections/community-cta.tsx` and `rules-page.tsx` for responsive mobile widths (`w-full sm:w-auto`).
- [x] `P6-06`: Update `apps/web/src/app/announcements/[slug]/page.tsx` footer action buttons to `flex-col sm:flex-row` and `w-full sm:w-auto`.
- [x] `P6-07`: Update `apps/web/src/components/layout/mobile-cta-bar.tsx` with `truncate px-2 text-center`.
- [x] `P6-08`: Update `apps/web/src/components/ui/carousel.tsx` button size to `h-11 w-11` on mobile.
- [x] `P6-09`: Run `make lint`, `make typecheck`, and `make build` to verify all checks pass.
